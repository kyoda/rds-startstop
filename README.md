# RDS start or stop

1. Install
  + install npm,pip
  + install package
    - `pip install awscli`
    - `npm install -g serverless`
2. `aws configure`
3. modify INSTANCEID and schedule in serverless.yml
4. `sls deploy`


